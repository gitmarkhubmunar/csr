import React, { Component } from 'react'
import isMobile from 'is-mobile';

class Card extends Component {
    render () {
        return (
            <div>
                I am a card.
            </div>
        )
    }
}

export default Card
